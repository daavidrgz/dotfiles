# Reduce scroll speed on Wayland
--enable-features=PercentBasedScrolling
--enable-smooth-scrolling

# Always expose CDP so archy / chrome-cdp can drive the browser
--remote-debugging-port=9222
--user-data-dir=/home/david/.config/chrome-cdp-profile
